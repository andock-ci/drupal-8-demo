- name: Composer install
  command: fin rc -T composer install
  args:
    chdir: "{{ project_path }}"

- name: fin init
  command: fin init
  args:
    chdir: "{{ docroot_path }}"
  when: environment_exists_before == false

