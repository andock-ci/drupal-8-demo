- name: drush sql-create
  command: fin exec -T "drush sql-create -y"
  args:
    chdir: "{{ docroot_path }}"
  when: instance_exists_before == false

- name: drush si
  command: fin exec -T "drush si minimal -y"
  args:
    chdir: "{{ docroot_path }}"
  when: instance_exists_before == false