- name: drush sql-create
  command: fin exec -T "drush sql-create -y"
  args:
    chdir: "{{ docroot_path }}"

- name: drush sql-sync
  command: fin exec -T "drush si -y"
  args:
    chdir: "{{ docroot_path }}"